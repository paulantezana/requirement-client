// ------------------------------------------------------------------------------
// PEDIDO COMPROBANTE DE SALIDA -------------------------------------------------
// ------------------------------------------------------------------------------
const exitVoucher = async ({response, setting, logoBase64})=> {
    return new Promise((resoleve,reject)=>{

    })
}

export default purchaseOrder;